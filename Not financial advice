
---

### openapi.yaml (mẫu tối giản — giúp Recall index API)
```yaml
openapi: 3.0.3
info:
  title: TradeVala Agent API
  version: 0.1.0
servers:
  - url: https://api.tradevala.example.com
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
  /v1/query:
    post:
      summary: Query the agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                prompt:
                  type: string
                symbol:
                  type: string
      responses:
        '200':
          description: Query result
components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
security:
  - BearerAuth: []
